#include "match.h"
#include "QString"

Match::Match():
    result(result),
    deep(Evaluation()),
    quick(Evaluation()),
    white(white),
    black(black),
    priority(priority),
    position(position)
{

}
